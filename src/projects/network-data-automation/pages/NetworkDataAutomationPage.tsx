import { ProjectShell } from '@/shell/components/ProjectShell'
import type { ProjectPageProps } from '@/projects/types'

export function NetworkDataAutomationPage(props: ProjectPageProps) {
  return <ProjectShell {...props} />
}
